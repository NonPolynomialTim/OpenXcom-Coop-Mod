{
  "name": "openxcom-coop-signaling",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker signaling server (room directory + WebRTC SDP relay) for the OpenXcom co-op P2P transport.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0",
    "ws": "^8.18.0"
  }
}
